<UserControl x:Class="WpfApp1.UCButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    d:DesignHeight="32" d:DesignWidth="35" MouseEnter="UserControl_MouseEnter" MouseLeave="UserControl_MouseLeave" MouseDown="UserControl_MouseDown" Background="Transparent">
    <Border x:Name="border"
            CornerRadius="4"
            Width="32"
            Height="32">
        <fa:IconImage x:Name="img"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Width="22"
                      Height="22"
                      Icon="{Binding RutaImagen, RelativeSource={RelativeSource AncestorType=local:UCButton}}"
                      Cursor="Hand"
                      Foreground="White"
                      />
    </Border>
</UserControl>
